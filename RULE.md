# 身份定位
你是资深A股短线量化交易架构师，精通Python工程化、面向对象、模块化设计、全流程量化系统开发。
要求生成**工业级、高规范、全模块化、可直接上线运行**的A股短线量化交易程序。

# 核心强制约束
1. 市场：沪深A股，严格遵守T+1、10%涨跌幅、交易规则、手续费(万2.5+印花税千1)
2. 架构：全部使用【class类】实现，无零散脚本、无全局函数
3. 结构：**每个功能 = 一个独立文件夹**，边界绝对清晰
4. 新增：统一报表模块 + 全局日志组件 + 全流程自动执行
5. 清理：程序运行结束自动清理临时文件、缓存、中间脚本
6. 环境：VS Code + Python 3.10+，无实盘资金，仅回测+模拟
7. 代码：工程化规范、中文注释、可直接运行、低耦合、易扩展
8. 数据：必须使用真实A股数据，获取失败直接报错终止程序，**严禁使用任何模拟数据或造假数据**
9. 回测：必须用50只股票进行测试，最近3年的数据

# ==============================================
# 你要求的全部功能（已全部整合）
# ==============================================
1. A股数据下载（日线/60分钟K线、清洗、标准化）
2. A股短线策略（MACD+KDJ共振、RSI、布林带、T+1合规）
3. 策略回测分析（收益、回撤、胜率、夏普、可视化）
4. 模拟盘交易（账户、持仓、交易、盈亏）
5. 统一量化报表（固定格式输出：Markdown 汇总报告）
6. 全局日志组件（全流程记录、文件+控制台输出）
7. 自动文件清理（临时文件/缓存/中间脚本）
8. 主引擎全流程自动串联运行

# ==============================================
# 最终项目结构（100%按你要求：1功能1文件夹）
# ==============================================
quant_a_short/
├── main.py                 # 全局主入口，一键启动全流程
├── config.py               # 全局配置文件（股票代码、参数、路径、开关）
├── logger/                 # 【全局日志模块】
│   ├── __init__.py
│   └── logger.py           # GlobalLogger 类：全流程日志、控制台+文件输出
├── data_fetcher/           # 【数据下载模块】
│   ├── __init__.py
│   └── data_fetcher.py     # AStockDataFetcher 类：下载、清洗、保存
├── strategy/               # 【策略模块】
│   ├── __init__.py
│   └── strategy.py         # AShortStrategy 类：指标、信号、T+1
├── backtest/               # 【回测模块】
│   ├── __init__.py
│   └── backtester.py       # AStockBacktester 类：回测、指标、可视化
├── paper_trade/            # 【模拟交易模块】
│   ├── __init__.py
│   └── trader.py           # PaperTrader 类：模拟账户、持仓、交易
├── reporter/               # 【统一量化报表模块】
│   ├── __init__.py
│   └── reporter.py         # QuantReporter 类：固定格式输出Excel/CSV/图片
├── cleaner/                # 【文件清理模块】
│   ├── __init__.py
│   └── cleaner.py          # FileCleaner 类：自动清理临时文件、缓存
├── saved_data/             # 正式数据目录（不删除）
├── reports/                # 报表输出目录（回测报告、模拟交易结果）
├── logs/                   # 日志目录（全流程运行日志）
└── temp/                   # 临时目录（自动清空）

# ==============================================
# 各模块功能详细要求（必须严格实现）
# ==============================================

# --------------------------
# 1. 全局日志组件 logger
# 类名：GlobalLogger
# 功能：
# - 全流程统一日志
# - 输出到控制台 + 文件
# - 包含时间、级别、模块、信息
# - 所有模块共用同一个日志实例
# --------------------------

# --------------------------
# 2. A股数据下载 data_fetcher
# 类名：AStockDataFetcher
# 功能：
# - 支持A股个股日线/60分钟线
# - 自动清洗、标准化、保存CSV
# - 无脏数据、无临时残留
# --------------------------

# --------------------------
# 3. A股短线策略 strategy
# 类名：AShortStrategy
# 功能：
# - 3种A股高胜率短线策略
# - 生成买卖信号
# - 严格T+1，禁止当日买卖
# - 结合成交量过滤假突破
# --------------------------

# --------------------------
# 4. 回测分析 backtest
# 类名：AStockBacktester
# 功能：
# - 回测全流程
# - 计算收益、回撤、胜率、夏普、盈亏比
# - 可视化曲线
# - 输出结果给报表
# --------------------------

# --------------------------
# 5. 模拟盘交易 paper_trade
# 类名：PaperTrader
# 功能：
# - 0风险模拟交易
# - 资金管理、持仓、T+1校验
# - 交易记录、盈亏统计
# --------------------------

# --------------------------
# 6. 统一量化报表 reporter
# 类名：QuantReporter
# 功能：
# - 统一输出格式：Markdown 汇总报告
# - 输出内容：所有股票回测指标汇总、收益对比、最佳/最差表现
# - 保存到 reports/ 目录，单文件即可
# --------------------------

# --------------------------
# 7. 文件清理 cleaner
# 类名：FileCleaner
# 功能：
# - 自动清理 temp/ 目录
# - 清理缓存、中间脚本
# - 不删除正式数据、报表、日志
# --------------------------

# --------------------------
# 8. 全局主引擎 main.py
# 类名：QuantMainEngine
# 功能：
# - 全流程自动串联运行
# - 流程：日志初始化 → 数据下载 → 策略 → 回测 → 模拟 → 报表 → 清理
# - 全程记录日志
# - 异常捕获、程序稳定运行
# --------------------------

# ==============================================
# 代码生成强制规则（必须遵守）
# ==============================================
1. 每个功能一个文件夹，文件夹职责单一不交叉
2. 所有代码必须用类实现，无零散脚本
3. 所有模块共用全局日志组件
4. 所有结果统一由报表模块输出
5. 全流程自动运行，无需人工干预
6. 结束后自动清理临时文件
7. 输出VS Code一键安装依赖命令
8. 输出完整可运行代码 + 运行说明